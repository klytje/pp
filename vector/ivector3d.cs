public interface ivector3d {
	//vector3d vprod(vector3d v);
	//double vdot(vector3d v);
	
	// Neither can be implemented here, as it requires access to the vector3d class. Granting this access would ruin the point of having this in a separate file (maybe that's where I went wrong)
}
